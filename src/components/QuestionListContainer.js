import QuestionList from "./QuestionList";

const QuestionListContainer = () => {

    return (
        <div>
            <h1>Question Lists (Unanswered / Answered)</h1>
            <div>
                <QuestionList />
            </div>
        </div>
    );
};

export default QuestionListContainer;